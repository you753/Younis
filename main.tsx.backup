import { createRoot } from "react-dom/client";
import "./index.css";

// Set document direction to RTL
document.documentElement.dir = "rtl";
document.documentElement.lang = "ar";

// Simple test component to check if React is working
function TestApp() {
  return (
    <div className="min-h-screen bg-gray-50 flex items-center justify-center" dir="rtl">
      <div className="bg-white p-8 rounded-lg shadow-lg max-w-md w-full text-center">
        <h1 className="text-3xl font-bold text-blue-600 mb-4">المحاسب الأعظم</h1>
        <p className="text-gray-600 mb-6">نظام محاسبة احترافي متكامل</p>
        <div className="grid grid-cols-1 gap-4">
          <div className="bg-blue-50 p-4 rounded">
            <h3 className="font-semibold text-blue-800">المبيعات</h3>
            <p className="text-sm text-blue-600">إدارة الفواتير والعملاء</p>
          </div>
          <div className="bg-green-50 p-4 rounded">
            <h3 className="font-semibold text-green-800">المشتريات</h3>
            <p className="text-sm text-green-600">إدارة الموردين والطلبات</p>
          </div>
          <div className="bg-purple-50 p-4 rounded">
            <h3 className="font-semibold text-purple-800">المخزون</h3>
            <p className="text-sm text-purple-600">تتبع المنتجات والكميات</p>
          </div>
        </div>
      </div>
    </div>
  );
}

const rootElement = document.getElementById("root");
if (rootElement) {
  createRoot(rootElement).render(<TestApp />);
} else {
  console.error("Root element not found!");
}
